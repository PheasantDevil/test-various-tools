jest.mock('../../../utils/api');

describe('issueService', () => {
  beforeEach(() => {
    jest.resetAllMocks();
  });

  test('getIssues should fetch issues from GitHub API', async () => {
    // テスト実装
  });

  // 他のテスト
});
